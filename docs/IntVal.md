# IntVal

- `v::intVal()`

Validates if the input is an integer.

```php
v::intVal()->validate('10'); //true
v::intVal()->validate(10); //true
```

See also:

  * [NumericVal](NumericVal.md)
  * [Digit](Digit.md)
